# EnterpriseRAG Backend Dependencies
# Core packages use >= pins so pip can resolve on any Python 3.11/3.12 environment.
# Production-only packages (document processing, ML) are in requirements.prod.txt.

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.27.0

# Environment Variables
python-dotenv>=1.0.0

# JSON handling
orjson>=3.9.0

# Async support
aiofiles>=23.2.1

# Logging
structlog>=24.1.0

# Rate limiting
slowapi>=0.1.9

# Observability
prometheus-fastapi-instrumentator>=6.3.0

# Authentication (JWT + password hashing)
# bcrypt must be <4.0.0 — passlib reads bcrypt.__about__ which was removed in 4.x
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0
